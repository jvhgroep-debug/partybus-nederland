---
import CityLayout from '../../../layouts/CityLayout.astro';
import ZuidHollandPartnerCityTemplate from '../../../components/cities/ZuidHollandPartnerCityTemplate.astro';
import { rotterdamCity as city } from '../../../data/cities/rotterdam';
import {
	buildTourgoWhatsappHref,
	resolveTourgoCityMedia,
	tourgoPartybussen,
} from '../../../data/partners/tourgoPartybussen';
import { zuidHollandFooter } from '../../../data/settings/regionalFooters';
import { resolveCityHeroImage } from '../../../lib/cityImages';
import { buildZuidHollandWhatsappTrackingContext } from '../../../lib/partnerWhatsappTracking';

const heroImage = resolveTourgoCityMedia(city.slug).hero;
const originalHeroImage = resolveCityHeroImage(city.slug);

const webPageLd = {
	'@context': 'https://schema.org',
	'@type': 'WebPage',
	name: city.metaTitle,
	description: city.metaDescription,
	url: city.canonical,
	inLanguage: 'nl-NL',
	isPartOf: {
		'@type': 'WebSite',
		name: 'Partybus Nederland',
		url: 'https://www.partybusnederland.nl/',
	},
};

const breadcrumbLd = {
	'@context': 'https://schema.org',
	'@type': 'BreadcrumbList',
	itemListElement: [
		{
			'@type': 'ListItem',
			position: 1,
			name: 'Home',
			item: 'https://www.partybusnederland.nl/',
		},
		{
			'@type': 'ListItem',
			position: 2,
			name: 'Steden',
			item: 'https://www.partybusnederland.nl/#steden',
		},
		{
			'@type': 'ListItem',
			position: 3,
			name: city.h1,
			item: city.canonical,
		},
	],
};

const faqLd = {
	'@context': 'https://schema.org',
	'@type': 'FAQPage',
	mainEntity: city.faqs.map((faq) => ({
		'@type': 'Question',
		name: faq.question,
		acceptedAnswer: {
			'@type': 'Answer',
			text: faq.answer,
		},
	})),
};

const ogImage = `https://www.partybusnederland.nl${originalHeroImage.src}`;
---

<CityLayout
	title={city.metaTitle}
	description={city.metaDescription}
	canonical={city.canonical}
	preloadImage={heroImage.src}
	ogTitle={city.metaTitle}
	ogDescription={city.metaDescription}
	ogUrl={city.canonical}
	ogImage={ogImage}
	twitterTitle={city.metaTitle}
	twitterDescription={city.metaDescription}
	twitterImage={ogImage}
	jsonLd={[webPageLd, breadcrumbLd, faqLd]}
	pageWhatsapp={{
		href: buildTourgoWhatsappHref(city.name),
		label: tourgoPartybussen.publicProfile.whatsapp.ariaLabel,
	}}
	regionalFooter={zuidHollandFooter}
	partnerWhatsappTracking={buildZuidHollandWhatsappTrackingContext({
		municipalitySlug: city.slug,
		municipalityName: city.name,
		pagePath: city.path,
		pageType: 'municipality',
	})}
>
	<ZuidHollandPartnerCityTemplate city={city} />
</CityLayout>
