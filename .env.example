# E-mail / Resend — verplicht voor werkende offerteverzending
# Cloudflare Pages → Settings → Environment variables:
#   RESEND_API_KEY = (bestaande key)
#   FROM_EMAIL = Partybus Nederland <info@starlocal.nl>  (na domeinverificatie in Resend)
#
# Zonder FROM_EMAIL gebruikt de code: Partybus Nederland <onboarding@resend.dev>
# Zet GEEN @partybusnederland.nl tot DNS/afzenderdomein live is.

RESEND_API_KEY=
# FROM_EMAIL=Partybus Nederland <info@starlocal.nl>

# Inbox (vast in code): info@starlocal.nl
