# Cloudflare Pages / Workers — optional local wrangler config
name = "partybus-nederland"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Set secrets via Cloudflare dashboard or `wrangler pages secret`:
# RESEND_API_KEY
# FROM_EMAIL
